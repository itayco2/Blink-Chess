"""The Lichess BOT (plan P9): configs, public-API snapshot, the stop rule and the pause.

The agent never starts the bot, never creates or reads its token, and never reads the bot's
environment. Its only action on a running bot is stopping the process by PID (pause.py).
"""
