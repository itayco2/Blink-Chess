"""The baseline ladder's learned rungs: a linear model and an MLP over 768 piece-square bits (plan P3).

Rung 0 (random) and rung 1 (material) live in blink.play.agents; rungs 2 and 3 live here. Every
baseline plays through the same ValueAgent and rules as Blink's value mode.
"""
