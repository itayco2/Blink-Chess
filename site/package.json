{
  "name": "blink-site",
  "version": "0.1.0",
  "private": true,
  "description": "Blink in the browser: one look (policy), onnxruntime-web on single-thread WASM.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "chess.js": "1.4.0",
    "cm-chessboard": "8.14.0",
    "onnxruntime-web": "1.30.0"
  }
}
